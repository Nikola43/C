#include <stdio.h>

main()
{
    int num = 0;
    do
    {
      printf("El numero es: %i\n",num);
      num++;
    }while(num < 7); ///El bucle se ejecutara minimo una vez, al final
                     ///evalua la condicion, y se repetira mientras la condicion sea verdadera
}///Fin main
