#include <stdio.h>
main()
{
      int numero = 10; /* Asignamos un valor de 10 para la variable numero*/
      
      numero += 5;  /* Asiganos el numero + 5 */
      printf("El valor de numero += 5 es %d.\n",numero);
      system("pause");
      
      numero -= 3;  /* Asiganos el numero - 3 */
      printf("El valor de numero -= 3 es %d.\n",numero);
      system("pause");
      
      numero *= 3;  /* Asiganos el numero * 3*/
      printf("El valor de numero *= 3 es %d.\n",numero);
      system("pause");
      
      numero /= 5;  /* Asiganos el numero /= 5 */
      printf("El valor de numero /= 5 es %d.\n",numero);
      system("pause");
      
      numero %= 3;  /* Asiganos el numero % 5 */
      printf("El valor de numero %%= 5 es %d.\n",numero);
      system("pause");
      
      }
