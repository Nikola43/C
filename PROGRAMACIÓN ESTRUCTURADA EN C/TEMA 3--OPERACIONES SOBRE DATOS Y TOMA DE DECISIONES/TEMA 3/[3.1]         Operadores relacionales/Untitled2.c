#include <stdio.h>
#include "acceso.h"
main()
{
    float valor;

    valor = (3<2); ///Si el operador condicinal es verdadero muestra un 0.
    printf("3>5 es %0.0f",valor);
    valor = (3<6); ///Si el operador condicinal es verdadero muestra un 1.
    printf("\n3<6 es %0.0f",valor);
    BLANCO
    BLANCO
    PAUSA;
}
